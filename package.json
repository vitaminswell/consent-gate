{
  "name": "consent-gate",
  "version": "1.0.4",
  "description": "A first-party cookie consent gate for Webflow and static sites. Blocks scripts and embeds before consent, speaks Google Consent Mode v2, no subscription.",
  "main": "dist/consent-gate.js",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node build.js",
    "test": "node test/verify.js && node build.js && MIN=1 node test/verify.js"
  },
  "keywords": [
    "cookie-consent",
    "gdpr",
    "webflow",
    "consent-mode",
    "cmp",
    "privacy"
  ],
  "homepage": "https://github.com/vitaminswell/consent-gate",
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.62.1",
    "terser": "^5.50.0"
  }
}
